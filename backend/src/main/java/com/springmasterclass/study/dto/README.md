# DTO

## Cần làm

- Tạo request/response DTO theo `frontend/pages/API_CONTRACT.md`.
- Đặt tên rõ nghĩa: `...Request`, `...Response`.
- Dùng validation annotation (`@NotBlank`, `@NotNull`, `@Size`, ...).
- Tách DTO create/update khi rule khác nhau.
- Không expose trực tiếp entity ra API.
