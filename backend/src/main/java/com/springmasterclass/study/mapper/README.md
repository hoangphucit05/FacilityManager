# Mapper

## Cần làm

- Tạo mapper giữa entity và DTO.
- Chuẩn hóa convert null-safe, parse date/number đúng kiểu.
- Hỗ trợ map cho cả create và update (patch field hợp lệ).
- Nếu dùng MapStruct, cấu hình component model `spring`.
- Viết unit test cho mapper quan trọng.
