# Config

## Cần làm

- Cấu hình datasource, JPA, migration (Flyway/Liquibase).
- Cấu hình CORS cho frontend.
- Cấu hình security cơ bản (authn/authz) theo role.
- Cấu hình logging, timezone, format JSON.
- Tách config theo profile: local/dev/prod.
